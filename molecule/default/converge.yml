---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Replace this task with one that validates your content
      ansible.builtin.debug:
        msg: "This is the effective test"
    - name: Additionally one-off tasks can be used to prep images for role application.
      ansible.builtin.debug:
        msg: "in case there is any non-role setup needed on the box before the test is run"
